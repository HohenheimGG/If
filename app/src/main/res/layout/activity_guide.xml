<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/apk/res/com.hohenheim"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="60dp"
        android:id="@+id/fr_layout_module"
        >

    </FrameLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@color/white"
        android:orientation="horizontal"
        android:layout_gravity="bottom"
        >

        <com.hohenheim.view.IconView
            style="@style/icon_view_bottom"
            tools:icon_text_size="12sp"
            tools:icon_text="@string/icon_recommend"
            tools:icon_icon="@drawable/nav_recommend"
            tools:icon_color="@color/theme_main_ornament"
            android:id="@+id/iv_label_recommend"
            />

        <com.hohenheim.view.IconView
            style="@style/icon_view_bottom"
            tools:icon_text_size="12sp"
            tools:icon_text="@string/icon_tool"
            tools:icon_icon="@drawable/nav_tool"
            tools:icon_color="@color/theme_main_ornament"
            android:id="@+id/iv_label_tool"
            />

    </LinearLayout>

</FrameLayout>
